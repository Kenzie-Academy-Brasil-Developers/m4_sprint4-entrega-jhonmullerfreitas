version: "3.9"

services:
  api: 
    container_name: express_api_entrega
    build: .
    ports:
      - 3001:3000
    environment:
      - PORT:3000
    depends_on: 
      db:
        condition: service_healthy
    volumes:
      - .:/app
  db:
    container_name: db
    image: postgres:14.3-alpine
    ports:
      - 5431:5432
    environment:
      - POSTGRES_USER=jhon
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=products_management
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jhon -d products_management"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s